server:
  port: 8080

spring:
  application:
    name: reefTMS-Core
  datasource:
    url: jdbc:mysql://localhost:3306/dev
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update

app:
  jwt:
    secretKey: ${JWT_SECRET_KEY}